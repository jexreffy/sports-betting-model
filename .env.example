# CollegeFootballData.com API key (required for CFB ingest)
CFBD_API_KEY=

# Optional: override local data root
SBM_DATA_DIR=data

# Optional: The Odds API (line provider stub — not required for v1)
THE_ODDS_API_KEY=
